export class CampusBuilding {
  name: string;
  x: number;
  y: number;
  description: string;

  constructor(name: string, x: number, y: number, description: string) {
    this.name = name;
    this.x = x;
    this.y = y;
    this.description = description;
  }
}
